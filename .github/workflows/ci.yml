name: my first pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: uptime
        run: uptime
      - name: Number processor
        run: nproc

test:
  runs-on: ubuntu-latest
  steps:
    - name: hello
      run: echo hello          
